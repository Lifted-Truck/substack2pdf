# Never commit cookies — they are credentials
cookies.txt
*.cookies
cookies*.txt

# Generated PDFs (this is a PDF generator — don't commit its output)
*.pdf
output/

# Saved HTML pages used as input
*.html

# Python / OS cruft
__pycache__/
*.pyc
.venv/
venv/
.DS_Store

# Harness: local verify state (kit). NOTE: do NOT ignore .kit-currency-plant-*
# — ignoring it blinds the gate-fires probe (kit CHANGELOG 2.3.0).
.harness/
